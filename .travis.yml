language: python
python:
 - "3.3"
 - "3.4"
 - "3.5"
 - "3.6"

install:
 - pip install -r dev-requirements.txt
 - pip install -r requirements.txt
 - pip install .

script:
 - cd tests && py.test -c pytest-cov.ini  -v
