language: python
python:
# - "3.3" # ignored due to tornado requirement of python >= 3.4
 - "3.4"
 - "3.5"
 - "3.6"

install:
 - pip install -r dev-requirements.txt
 - pip install -r requirements.txt
 - pip install coveralls
 - pip install .

script:
 #- cd tests && py.test -c pytest-cov.ini  -v
 #- cd tests && coverage report -m
 - coverage run --source . -m py.test tests/

after_success:
  - coveralls
